# Ohne diese Werte läuft die App im Demo-Modus (Daten nur im Browser, KI simuliert).
# Beide Werte stehen im Supabase-Dashboard unter Project Settings > API. Sie sind öffentlich (Browser),
# der Schutz der Daten läuft über Row Level Security.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
